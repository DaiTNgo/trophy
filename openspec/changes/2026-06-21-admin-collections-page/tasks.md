1. Add admin collections list page with Medusa-like table layout, search, and empty states.
2. Add collection create and edit flows with title, handle, metadata, and product membership sections.
   - [x] Follow-up: move collection media editing out of the edit drawer and onto the main detail page so operators can update media faster.
3. Add validators for required title, unique handle, and safe product assignment changes.
4. Add mock collection repository and service contracts that can later map to backend endpoints.
5. Add tests for handle generation, duplicate handle rejection, and collection membership updates.
