# Session Handoff

The change `separate-variant-customization-media` is fully specified and ready for implementation.

Read these files first:

- `proposal.md` for scope and rationale.
- `design.md` for persistence, API, readiness, admin, and storefront decisions.
- `specs/variant-customization-media/spec.md` for normative scenarios.
- `tasks.md` for the implementation checklist.

The repository is in dev mode. Do not add a compatibility migration or preserve the old first-gallery-image canvas behavior. Existing ADR/context updates explain the domain decision; active implementation must follow the new OpenSpec contract.
