# Repo Progress

## Current State
- Standardized admin product save errors on 2026-07-12. Create product submit now shows a Medusa toast for validation failures and save/upload failures. Product detail save/update actions for overview, organization, attributes, customization enable/disable, publish/archive, media thumbnail save, variant price/stock/create/edit/delete, and option save validation now surface the same error message through `toast.error(...)` while preserving existing inline errors. Verification: `pnpm --filter admin build` passes.
- Updated localized product field requirements on 2026-07-12. Backend publish validation now requires product title Vietnamese (`vi`) only and no longer blocks publish solely because product title English (`en`) is empty; subtitle and description remain optional localized fields. Updated `CONTEXT.md`, ADR `docs/adr/0006-bilingual-catalog-localization.md`, and the `bilingual-catalog-localization` OpenSpec spec/design/tasks to document the field-specific rule clearly. Added unit coverage in `apps/backend/src/routes/admin/products.test.ts` for missing English title allowed and missing Vietnamese title rejected. Verification: `pnpm --filter backend exec vitest run --config vitest.config.ts src/routes/admin/products.test.ts`, `pnpm --filter backend check`, and `openspec validate bilingual-catalog-localization --strict` pass. Note: `pnpm --filter backend test -- products.test.ts` also matches `products.route.test.ts`, whose existing query-chain mock failures remain unrelated to this rule.
- Fixed admin product-detail Media section on 2026-07-12. `ProductDetailThumbnail` now treats product media as the product thumbnail/media pool and derives it from both product-level media and unique variant media assets. Product media appears first, variant media remains available as selectable source media, and admins can still upload separate product media not attached to any variant. The FocusModal initial thumbnail list uses this merged pool, and multi-file upload now respects the file input's `multiple` behavior. Verification: `pnpm --filter admin build` passes.
- Updated admin product-detail edit overview on 2026-07-12 to match create-product required/optional rules. The edit drawer now marks product `Title` as requiring only Vietnamese, shows Subtitle and Description as optional, and passes `requiredLocales={[]}` for optional localized fields so missing EN/VI indicators do not appear for optional copy. Save now validates Vietnamese title client-side before calling the backend. Verification: `pnpm --filter admin build` passes.
- Updated admin product-detail create/edit variant on 2026-07-12. `apps/admin/src/pages/product-detail/product-detail-variants.tsx` now opens a Medusa `FocusModal` for variant create/edit instead of the previous Drawer. The modal is a single screen with Details (bilingual `Title`, optional `SKU`), Options, Price, Media upload/removal, Inventory quantity, and Allow backorder. Create sends bilingual title, price, inventory, media, backorder, and option selections in one `POST`; edit pre-fills the same fields and saves through the existing operation-specific variant routes for details, prices, stock, and media. Backend variant create/update/full-replace now accept `title` as either a legacy string or `{ vi, en }`, persist canonical Vietnamese to `product_variants.title`, and store translations under `catalog_translations` owner type `product_variant`. PDF media upload still uses the existing PDF-to-image conversion path. Verification: `pnpm --filter admin build` and `pnpm --filter backend check` pass.
- Fixed admin product-detail create-option payload shape on 2026-07-12. The product detail options modal was sending `POST /api/admin/products/:id/options` as `values: [{ vi, en }]`, but the backend route now validates `values: [{ value: { vi, en } }]` after the localized catalog contract changes. Updated the admin client helper and product-detail options modal to send the nested `value` object shape, and added a regression test in `apps/admin/src/lib/products-client.test.ts` to lock that request body. Verification: `pnpm --filter admin test -- products-client.test.ts` and `pnpm --filter admin build` pass.
- Replaced the drawer-based multi-option management with a single-option `FocusModal` styled with Medusa UI components on 2026-07-11. Clicking the dropdown menu next to an option triggers the "Edit Option" modal (prefilled with Option title and a tag-input for Variations/Values). Admins can also delete options via the dropdown or add a new option from the top card dropdown. The Variations input allows comma-separated entries, backspace to delete the last badge, and a close button on badges. Verification: `pnpm --filter admin build` and `./init.sh` both pass.
- Updated admin create-product Organize tab optional controls on 2026-07-11. Collection and Categories now use Medusa `Label` plus `Text as="span"` optional markers instead of embedding optional text in the label string. The single-select optional collection control now shows a Medusa secondary `Clear` button when selected, allowing admins to remove the chosen collection; categories remain optional via the existing multi-select control. Verification: `pnpm --filter admin build` passes.
- Fixed admin create-product required/optional field signaling on 2026-07-11. The existing product catalog specs already state `title` is required while `subtitle`, `description`, and admin-entered `handle` are optional; `handle` is generated from title when omitted. Backend create schemas already matched that rule, so the fix was limited to the admin localized-field UX: `LocalizedTextField` now accepts `requiredLocales`, create-product title only marks Vietnamese as required for draft/minimal input, and subtitle/description no longer show missing-locale indicators as if they were required. Verification: `pnpm --filter admin build` passes.
- Fixed the storefront PDP media gallery on 2026-07-11. `apps/storefront/app/routes/product.$handle.tsx` now drives `ProductGallery` from the full `selectedVariant.media[]` set instead of only `variant.media[0]`, so products with multiple images now render a real thumbnail strip/carousel below the preview. The selected gallery image also feeds `buildProductCustomizationTemplate(...)`, so customizable-product preview backgrounds switch with the chosen thumbnail instead of staying pinned to the first media item. Verification: `pnpm --filter router-cf build` passes.
- Removed the unused legacy storefront component `apps/storefront/app/components/CupCustomizer.tsx` on 2026-07-11 after confirming it had no remaining references. This completes the storefront active-surface token cleanup by deleting the last known unreferenced hardcoded-color component instead of carrying it forward. Verification: `rg -n "CupCustomizer" apps/storefront apps/admin packages` returns no matches and `pnpm --filter router-cf build` passes.
- Aligned the shared customization React package with the storefront semantic token system on 2026-07-11. `packages/customization-react/src/index.tsx` now uses semantic roles such as `accent`, `accent-foreground`, `surface-container-low`, `surface-container`, `surface-container-lowest`, `outline`, `outline-variant`, `on-surface`, `on-surface-variant`, and `destructive` instead of direct color literals; `apps/admin/tailwind.config.ts` now exposes the same generic vocabulary required by the shared package so admin and storefront render the same token contract. Verification: `pnpm --filter @trophy/customization-react check`, `pnpm --filter admin build`, and `pnpm --filter router-cf build` pass.
- Fixed storefront rendering for bilingual catalog API payloads on 2026-07-11. Storefront catalog types now accept `string | { vi, en }`, and home/category cards, navbar/mega/mobile menus, product listings, collection pages, product detail option/spec text, cart, and checkout resolve display text through `getLocalized(...)` before rendering. Home/layout/listing loaders now pass the active locale into storefront catalog requests. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Updated the storefront listing surfaces on 2026-07-11 to follow the supplied TrophySmack collection-page UI more closely. `apps/storefront/app/components/products/ProductListingShell.tsx` now owns a shared listing layout for both `/products` and `/collections/:handle`, with a dark hero band, trust-badge row, filter toolbar, denser product grid, and numbered pagination. `products.tsx` and `collections.$handle.tsx` both render through that shell, and listing filters now use category handles directly in `?category=` so URLs match the Navbar/category-link contract. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and local preview at `http://localhost:4173/`.
- Replaced recent storefront hardcoded TrophySmack colors with semantic Tailwind tokens on 2026-07-11. `apps/storefront/app/app.css` now exposes semantic token utilities for brand (`brand-hero`, `brand-strong`, `brand-support`, `brand-accent`), text, surface, border, and action roles (`action-support`, `action-positive`, `action-commerce` plus hover states), and `apps/storefront/tailwind.config.js` mirrors those tokens for config-driven consumers. Refactored the current storefront shell/home/shared/product/cart/checkout surfaces to use semantic utilities instead of direct hex values. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and targeted `rg` checks against touched storefront surfaces pass.
- Finalized the TrophySmack token role mapping on 2026-07-11 from the supplied cart/checkout/PDP/collection screenshots and synced the storefront implementation to match. `apps/storefront/app/components/home/HeroSection.tsx` and `NewsletterSection.tsx` now use blue (`#288AB6`) for supportive CTAs, `shared/ProductCard.tsx` and cart "Recently Viewed" prices now use orange (`#EA4222`) like the collection grid, `app/routes/cart.tsx` now uses blue for continue/checkout actions with neutral line pricing, and `app/routes/checkout.tsx` now uses teal (`#04A387`) for pay/selected-payment states while keeping order-summary prices/totals neutral. `apps/storefront/DESIGN.md` now documents the component-by-component role mapping. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Continued the TrophySmack storefront redesign on 2026-07-11 across the shopper shell and home/shared UI. Updated `apps/storefront/app/components/layout/Navbar.tsx`, `Footer.tsx`, `home/HeroSection.tsx`, `TrustBar.tsx`, `BestSellersSection.tsx`, `CategoriesSection.tsx`, `ManufacturerSection.tsx`, `NewsletterSection.tsx`, `ProofRow.tsx`, `ReviewsSection.tsx`, `ShopByOccasionSection.tsx`, `shared/ProductCard.tsx`, `shared/Pagination.tsx`, and `products/FilterChips.tsx` so the home page, global navigation/footer, collection/product browsing surfaces, and shared shopper UI all use the TrophySmack blue/orange/teal palette and Koulen display treatment. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Extended the TrophySmack storefront alignment on 2026-07-11 from product detail into cart and checkout. `apps/storefront/app/routes/cart.tsx` and `apps/storefront/app/routes/checkout.tsx` now use the TrophySmack font hierarchy (`Koulen` display, `Open Sans` body), teal price treatment (`#04A387`), orange commercial CTAs (`#EA4222`), and dark-blue headings (`#244159`) while keeping existing cart/checkout behavior unchanged. Also cleaned the remaining old display-font comment in `app/components/home/HeroSection.tsx`. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Aligned the storefront design system on 2026-07-11 to the local TrophySmack reference at `/Users/dnt/Desktop/www.trophysmack.com/www.trophysmack.com/index.html`. Storefront now loads `Koulen` for display type and `Open Sans` for body copy, `apps/storefront/DESIGN.md` documents the TrophySmack palette/typography/layout rules, and core storefront tokens in `apps/storefront/app/app.css` plus `tailwind.config.js` now map to TrophySmack brand colors: landing dark blue `#171740`, dark blue `#244159`, blue `#288AB6`, teal `#04A387`, orange `#EA4222`, black `#232323`, and white `#FFFFFF`. Product detail CTA, price, and option headings were tuned to that system. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Simplified the storefront palette on 2026-07-11 to remove the remaining champagne-metal accent. `apps/storefront/DESIGN.md` now treats the canonical palette as white (`#ffffff`), ink navy (`#1a2233`), graphite (`#18161a`), cool slate (`#5b6470`), and mineral neutrals (`#ded8d0`, `#bcb4ab`, `#f7f5f2`, `#f1eeea`, `#e8e4de`, `#f3efea`). `apps/storefront/app/app.css` and `tailwind.config.js` were updated so `primary-container`, `secondary`, `inverse-primary`, and `primary-fixed` no longer point at metallic beige values. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Follow-up on 2026-07-11 redesigned the Product Detail page to exactly match the TrophySmack visual reference: added a horizontal gallery thumbnail strip with prev/next controls, flattened the customization side panel, implemented a "STEP X:" pattern in `@trophy/customization-react` with dense 6-col clipart grids and compact 32px color swatches, styled the quantity stepper and Add to Cart/Preview CTAs, and extracted short product descriptions into the header block. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Refined the storefront product-detail visual system on 2026-07-11 into a stricter white-luxury direction. `apps/storefront/DESIGN_2.md` now defines true-white page background, cool ink CTA (`#1a2233`), champagne-metal accent (`#d8c4a7`), mineral borders (`#ded8d0` / `#bcb4ab`), cooler supporting copy (`#5b6470`), toned-down shadow usage, and a 1680px workbench layout. Synced the implementation in `apps/storefront/app/app.css`, `tailwind.config.js`, `app/components/product/*`, and `app/routes/product.$handle.tsx` so the product page no longer mixes the old warm off-white system with the newer white shells. Product data flow, customization runtime, upload handling, validation, and cart behavior were unchanged. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Consolidated the storefront design docs on 2026-07-11. Merged the white-luxury direction from `apps/storefront/DESIGN_2.md` into `apps/storefront/DESIGN.md` and removed `DESIGN_2.md` so the storefront has one canonical design spec.
- Unified storefront product detail visual design on 2026-07-11. The product detail page now uses a cleaner white workbench background, max-width 1680px content frame, consistent 14px shell radius, warm tan borders (`#d8c1ad`), deep ink CTA color (`#110023`), blue-gray labels (`#2d4056`), and muted copy (`#7b6b5f`) across breadcrumbs, gallery shell, customization purchase panel, option/quantity controls, mobile action bar, and lower detail/trust sections. Product data flow, customization runtime, upload handling, validation, and cart behavior were unchanged. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass. Storefront dev server restarted at `http://localhost:5175/`.
- Updated the shared customization preview toolbar layout on 2026-07-11 to remove image-selection layout shift. `@trophy/customization-react` now keeps the preview header stable with mode and viewport zoom controls, and renders a fixed-height sticky action rail directly below it. The rail always occupies 76px: it shows guidance when no uploaded image is selected and swaps in crop/rotate/nudge/reset actions when one is selected, so the canvas does not jump. Verification: `pnpm --filter @trophy/customization-react check`, `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass. Storefront dev server restarted at `http://localhost:5175/`.
- Fixed storefront Tailwind generation for the shared customization React package on 2026-07-11. Admin already scanned `@trophy/customization-react` through Tailwind v3 `content` in `apps/admin/tailwind.config.ts`; storefront uses Tailwind v4 through `apps/storefront/app/app.css`, so it needed `@source "../../../packages/customization-react/src";` for shared package classes to be generated. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass; the storefront dev server was restarted on `http://localhost:5175/`.
- Updated shared customization form UI on 2026-07-11 to match the provided mug customization references while leaving the preview canvas/editor unchanged. `@trophy/customization-react` now owns the form presentation used by both storefront product detail and admin PreviewDialog: large plain labels, responsive 56px-style inputs, square color/font swatches, upload thumbnail + Replace/remove controls, and compact responsive sizing for admin's narrower panel. Existing data contracts, upload handling, crop behavior, validation, and cart snapshot logic were preserved. Verification: `pnpm --filter @trophy/customization-react check`, `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Extracted customization React UI into the new shared package `@trophy/customization-react` on 2026-07-10. The package owns the shadcn-style canvas/form UI, Edit/View preview mode, selected-image top toolbar actions, four-corner image scale handles, image upload field, clipart selector, text fields, shared font/asset URL resolvers, and a full `CustomizationStudio` entrypoint. Storefront product detail now imports preview/form from the package and only supplies backend upload/font callbacks. Admin PreviewDialog now uses the same package canvas/form while keeping admin-only shell actions for reset/export/close. Deleted the old storefront-local customization component and removed the old admin-local preview/form implementation. Verification: `pnpm --filter @trophy/customization-react check`, `pnpm --filter @trophy/admin build`, `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter customization check` pass.
- Added storefront GitHub CI/CD on 2026-07-10. New workflow `.github/workflows/storefront-ci-cd.yml` runs storefront/customization checks for PRs and `main` pushes, uploads a Cloudflare Workers preview version for each PR with alias `pr-<number>`, comments the preview URL back onto the PR, and deploys storefront production on push to `main`. Also set `preview_urls: true` in `apps/storefront/wrangler.jsonc`. Local verification: `ruby -e "require 'yaml'; YAML.load_file('.github/workflows/storefront-ci-cd.yml')"` and `./init.sh` both pass. Required GitHub secrets: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, and `STOREFRONT_BACKEND_URL`.
- Moved storefront uploaded-image adjustment actions to the preview top toolbar on 2026-07-10. Image action buttons now appear beside the Edit/View controls only after a shopper selects an uploaded image; the canvas itself keeps only the selected-image outline and four scale handles, so controls no longer cover the preview artwork. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Updated storefront product detail canvas/system design on 2026-07-10 so product detail reads as a unified personalization studio. The preview now has Edit/View modes, fit/zoom controls, View-mode pan/pinch zoom, and Edit-mode uploaded image crop behavior matching admin Preview: click an uploaded image to select it, drag inside the clipped image area to move the crop, use mouse wheel or canvas buttons to zoom, rotate/nudge/reset, and drag four corner handles to scale the image around the opposite corner. The gallery shell, setup panel, variant option buttons, and customization section were restyled into one studio/workflow visual system. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter customization test`, `pnpm --filter customization check`, and `pnpm --filter router-cf build` pass.
- Updated storefront product customization image editing on 2026-07-10 so image adjustment actions live inside the preview canvas, matching the admin Preview pattern more closely. Uploaded image controls were removed from the form card; the form now only uploads/replaces/removes the image and tells shoppers to select the image in preview. The live preview shows a floating crop toolbar for the selected uploaded image with zoom, rotate, directional nudges, and reset, while drag-to-pan remains on the image itself. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter customization test`, `pnpm --filter customization check`, and `pnpm --filter router-cf build` pass.
- Refined the storefront product detail redesign on 2026-07-10 after UI feedback. Variant selection now renders by product option group instead of a flat variant list, and option groups with only one value are hidden. The purchase panel keeps quantity and CTA, while the form area is reserved for customization fields. Storefront uploaded image customization now supports direct drag-to-pan in the live preview plus button controls for zoom in/out, rotate left/right, move left/right/up/down, reset, and remove. Added optional `cropRotationDeg` to the shared customization image value/runtime so rotation is preserved in the shopper customization snapshot. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter customization test`, `pnpm --filter customization check`, and `pnpm --filter router-cf build` pass.
- Implemented the storefront product detail redesign on 2026-07-10 in `apps/storefront/app/routes/product.$handle.tsx` and product components. The page now uses a personalization-first layout inspired by Customily: sticky live preview stage with status chips, purchase panel separated from product details, shopper-style customization field cards, larger clipart choices, upload picker with thumbnail, and full-width details/trust sections below the hero. Backend route contracts, product/customization data flow, Contact Price rules, selected-variant background behavior, validation, upload endpoint, and cart snapshot behavior were left unchanged. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Updated the admin customization Preview field layout on 2026-07-10 in `apps/admin/src/components/customization/customization-template-preview.tsx`. Only the right-side preview field controls changed: text/image fields now render as shopper-style cards, upload uses a styled image picker with thumbnail and compact crop actions, clipart choices use a larger selectable grid, and grouped font/color/format controls are visually separated. Preview canvas, modal shell, crop math, PDF export, and data contracts were left unchanged. Verification: `pnpm --filter admin build` passes.
- Grilled and captured the Medusa-style product option/variant domain decisions on 2026-07-06 in `CONTEXT.md`. Trophy now treats `Default option` / `Default option value` as real option data equivalent to an operator-created option/value, keeps default option/value visible in admin and storefront selection logic, supports option rename and option value rename in-place, allows deleting option values even when variants use them, represents affected variants as `Missing value`, blocks new publish attempts while any variant is unreconciled, and disables invalid storefront option combinations while auto-reselecting the first valid variant when the current selection becomes invalid; auto-reselection uses variant position, then variant ID as the stable tie-breaker, and Contact Price variants remain valid selections, and out-of-stock variants remain selectable while purchase CTA reflects inventory/backorder availability; Contact Price CTA takes precedence over stock/backorder CTA, and Contact Price inquiry requires a valid selected variant while capturing option/customization snapshots without creating cart or order records.
- Captured a research note on Medusa Admin default option/variant behavior on 2026-07-05 in `docs/research/2026-07-05-medusa-admin-default-variant-behavior.md`. The note explains that Medusa Admin seeds `Default option`, `Default option value`, and `Default variant` in the create flow, that the detail page only renders persisted product data, that the Admin UI enforces at least one option/variant while the backend validator does not hard-require variants, and that Trophy should mirror the single default-variant invariant rather than the literal placeholder wording.
- Captured the proposed TrophySmack-inspired storefront home layout as `docs/plans/2026-07-05-storefront-home-trophysmack-layout-spec.md` on 2026-07-05. The spec defines the Premium Championship Commerce direction for the storefront home page: trust bar, header/search/category strip, full-bleed hero, proof row, product categories, best sellers, customization feature, occasion paths, real-work gallery, reviews, SEO intro, newsletter, and footer. Follow-up refined search/newsletter inputs to compact 40px sizing so the header stays lighter. This is design/spec work only; no storefront implementation files were changed in this pass.
- Fixed admin sign-in/session loading on 2026-07-05 after confirming Better Auth 1.6.23 `/get-session` reads the signed session cookie and does not resolve the `Authorization: Bearer` token returned by username sign-in. Local HTTP also cannot rely on the existing `sameSite: "none"` + `secure: true` cookie settings. Added backend `getAdminSession()` with Bearer-token fallback against the D1 `session` table, added `GET /api/admin/me`, updated admin session bootstrap/login refresh to call that endpoint instead of `authClient.getSession()`, and moved remaining backend admin guards to the shared resolver. Added route-surface regression coverage for `/api/admin/me` with bearer-only auth. Verification: `pnpm --filter backend test -- auth-session.test.ts`, `pnpm --filter backend check`, `pnpm --filter backend test`, `pnpm --filter backend build`, `pnpm --filter admin build`, and `./init.sh` all pass.
- Updated the agent harness on 2026-07-04 to require Hono RPC for new backend route contracts consumed by admin or storefront. `AGENTS.md` now says new client integrations should export backend route/app types, use `hc<AppType>()`, avoid new hand-written fetch wrappers unless a blocker is documented, and return explicit `c.json(payload, status)` responses for typed success/error cases instead of untyped client-consumed not-found responses. Verification: `node -e "JSON.parse(...feature_list.json...)"` and targeted `rg` checks.
- Fixed admin product detail publish status display on 2026-07-04. The backend publish route was already updating and returning lowercase `status: "published"`, but the admin product API mapper typed/checked PascalCase `"Published"` and therefore mapped the refreshed product back to UI status `Draft`. Updated the admin mapper to normalize backend lowercase statuses before rendering. Added `apps/admin/src/lib/products-client.test.ts` covering the lowercase published API response. Verification: `pnpm --filter admin test -- products-client.test.ts`, `pnpm --filter admin build`, and `./init.sh` all pass.
- Updated the root harness on 2026-07-04 so backend API work is only done when it has API contract coverage at the public admin/storefront route surface, important failure-mode coverage, service/helper unit tests for non-trivial business rules, and recorded risk when tests are intentionally omitted. Added `pnpm --filter backend test` to the documented backend checks and updated `init.sh` to run backend check, backend test, and backend build before admin/storefront verification. Verified with `pnpm --filter backend check`, `pnpm --filter backend test`, `pnpm --filter backend build`, and `./init.sh`.
- Implemented Admin UI for Collections and Categories management on 2026-07-04. Replaced the placeholder pages with real Medusa UI CRUD pages (`CollectionsListPage`, `CollectionDetailPage`, `CategoriesListPage`, `CategoryDetailPage`) that fetch and manage data via the `backendFetch` utility. Also added missing `PUT` and `DELETE` backend routes to `/api/admin/product-metadata/(collections|categories)`. Verified via `./init.sh`.
- Storefront SSR instability was investigated on 2026-07-04. The failure reproduced with `pnpm --filter router-cf preview`: `/products` returned 500 while `/`, `/cart`, `/checkout`, and `/customize` returned 200. Browser automation reloads on the successful routes showed no hydration warnings or page errors. Root cause was the storefront's local backend fallback using `http://127.0.0.1:8787` while the backend dev server in this environment responds at `http://localhost:8787` and direct `127.0.0.1:8787` connection fails. Updated storefront backend fallback URLs to `http://localhost:8787`; after rebuilding preview, `/products` returned 200 and browser reloads on `/products`, `/`, and `/customize` produced no console errors. `./init.sh` passes after the fix.
- Root harness is now a fallback index for non-OpenSpec work only.
- OpenSpec changes should keep their own `tasks.md`, `progress.md`, and `session-handoff.md` inside `openspec/changes/<change>/`.
- `./init.sh` remains the baseline verification entrypoint.
- Product-owned customization planning was captured on 2026-07-03. The domain glossary now defines customizable product/background/canvas readiness terms, ADR `docs/adr/0001-product-owned-customization.md` records the decision to replace standalone customization template/revision lifecycle with one-to-one product customization, and the PRD is published as `.scratch/issues/product-owned-customization-prd.md` with `ready-for-agent` metadata.
- OpenSpec change `product-owned-customization` was proposed on 2026-07-03 with proposal, design, specs, tasks, progress, and session handoff under `openspec/changes/product-owned-customization/`. `openspec validate product-owned-customization --strict` passes.
- Storefront product API planning was captured on 2026-07-03. The domain glossary now defines Storefront Product Listing, Storefront Product Search, Admin Product Catalog, and Contact Price; ADR `docs/adr/0002-storefront-product-api-boundary.md` records the public storefront/admin catalog API split; and the PRD is published as `.scratch/issues/storefront-product-apis-prd.md` with `ready-for-agent` metadata.
- OpenSpec change `storefront-product-apis` was proposed on 2026-07-03 with proposal, design, specs, tasks, progress, and session handoff under `openspec/changes/storefront-product-apis/`. The change is ready for implementation pending OpenSpec validation.

## Notes
- `pnpm --filter router-cf dev` currently exits with `React Router Vite plugin not found in Vite config` in this environment, so the restartable local check for this pass is `pnpm --filter router-cf preview -- --port 4173` after a successful build.
- Storefront preview deploys rely on Cloudflare Workers Preview URLs. If preview URLs are later toggled in the Cloudflare dashboard, keep `apps/storefront/wrangler.jsonc` in sync or the next Wrangler deploy will overwrite the dashboard setting.
- Backend API changes should now treat route-level API contract tests as required evidence before admin/storefront integration depends on the endpoint.
- Do not use this file as the source of truth for parallel OpenSpec changes.
- If a change folder is missing local state files, create them alongside that change before starting work.
- Issue tracker setup is not configured and `gh` was unavailable in this environment, so the PRD was published as a local markdown issue.
- Updated storefront mobile menu layout on 2026-07-05 to use a collapsible grid structure based on the provided TrophySmack HTML snippet.
- Updated Product Metadata schema and API on 2026-07-06: Added `position` to `product_categories` to support manual ranking, kept `description` in `product_categories`, and removed `description` from `product_collections`. Decided NOT to add `parentId` to categories for now. Implemented `PUT /api/admin/product-metadata/categories/ranking` to batch update category rankings and wired it up to the `EditRankingModal` drag-and-drop feature in the admin app.
