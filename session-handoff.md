- 2026-08-18: Storefront customization upload assets are now deleted when shoppers remove, replace, or switch an uploaded image to clipart. `DELETE /api/storefront/customizations/assets/:id` validates upload token, shopper draft, and field ownership, then deletes source/preview R2 objects plus the row. UI retains its current value on deletion failure. An asset referenced by a browser-local cart line is intentionally retained because checkout resolves it by `assetId`; shopper-draft expiry cleans it later. The client uses the existing SSR-compatible `app/lib/api.ts` fetch boundary instead of Hono RPC because storefront does not establish a backend route-type dependency. Verification: full `./init.sh` passes (backend 243 tests, all checks/builds) and `git diff --check` passes. `feature_list.json` remains invalid baseline JSON and was not edited.

- 2026-08-18: Fixed local backend Vite preflight handling for storefront customization uploads. The Worker CORS middleware already allowed all `/api/storefront/*` request headers, but Vite intercepted local `OPTIONS` and its customization policy lacked `X-Shopper-Draft-Id` and `X-Shopper-Field-Id`. Both are now included with a regression assertion in `cors.test.ts`. Verified backend tests (39 files, 240 tests), backend typecheck, diff check, and a real temporary Vite preflight. Restart the backend Vite process on `8787` to load the updated server CORS configuration. `feature_list.json` remains invalid baseline JSON and was not edited.

- 2026-08-09: Public CORS now also covers `/api/assets/*`, mounted before the product/customization/brand asset routes. Preflights and binary responses allow every non-credentialed origin, fixing admin fetches of product asset `content` URLs; `cors.test.ts` verifies the product asset preflight. Backend test suite (231), backend check/build, and diff check pass. Deploy backend to apply in production. `feature_list.json` remains invalid baseline JSON.

- 2026-08-09: Storefront CORS is now public for all `/api/storefront/*` routes: any origin is allowed without credentials, and preflights reflect the requested method and headers. This fixes `POST /api/storefront/customizations/assets` with `X-Upload-Token`, `X-Shopper-Draft-Id`, and `X-Shopper-Field-Id`, including deployments at origins not listed in `STOREFRONT_APP_ORIGIN`. `cors.test.ts` covers the real upload preflight. Verification: backend test suite (230), backend check/build, admin build, and diff check pass. `./init.sh` still stops only at the pre-existing storefront address type error in `app/routes/checkout.tsx:305`. Deploy backend to apply in production; `feature_list.json` remains invalid baseline JSON.

- 2026-08-09: Removed WebP fetch-as-blob from admin `MediaPreview` and legacy `AdminMedia`. Public persisted image `contentUrl` values now go directly to `<img>`, avoiding the product-detail cross-origin authenticated fetch to `/api/assets/products/:assetId/content`; PDF preview still fetches source bytes for pdf.js rendering. Deleted the obsolete `shouldLoadMediaViaBlob` helper and its tests. Verification: `pnpm --filter admin test` (6 files, 21 tests), `pnpm --filter admin build`, and `git diff --check` pass. `feature_list.json` is invalid JSON from the existing worktree and remains untouched.

- 2026-08-09: Product Detail Customization Setup/Repair now reuses the embedded `CreateProductCustomization` workspace with the same bounded-panel behavior as Create Product. `CustomizationBackgroundModal`'s body is a `min-h-0` flex container with `overflow-hidden`; the Media tab owns its own `overflow-y-auto` table region, while the Custom editor uses Create Product's same `flex-1 min-h-0` wrapper and `px-6 py-6` content shell rather than forcing `h-[680px]`. Consequently, the reused LeftPanel and Inspector have independent vertical scrolling rather than scrolling the entire modal. Verification: `pnpm --filter admin test` (24 tests), `pnpm --filter admin build`, and `git diff --check` pass. `./init.sh` passes backend (229 tests) and admin build, then stops at the pre-existing storefront type error in `apps/storefront/app/routes/checkout.tsx:305` (`city` and `country` are missing). `feature_list.json` is invalid JSON from the existing worktree and remains untouched.

- 2026-08-09: Product Detail Manage Media converts every selected PDF's first page to WebP before sending it to the backend, for both Gallery Media and Customization Background replacement. The atomic Product Detail Create Variant flow applies the same conversion to Gallery Media and Customization Backgrounds prior to background dimension validation and multipart submission; its background input now accepts PDFs. Customization Setup and Repair now accept PDFs and normalize them to WebP before staged preview, readiness validation, and atomic activation/repair submission. Create Product already performed this conversion and was intentionally unchanged. `pdf-preview.test.ts` covers the first-page WebP output contract. Verification: `pnpm --filter admin test` (24 tests), `pnpm --filter admin build`, and `git diff --check` pass. `./init.sh` passes backend (219 tests) and admin work, then fails in unrelated existing storefront typechecking at `apps/storefront/app/routes/checkout.tsx:305`, where an address value lacks required `city` and `country` fields. `feature_list.json` is invalid JSON from the existing worktree and remains untouched.

- 2026-08-08: Added API contract coverage for the admin product customization lifecycle routes (`product-customization-lifecycle-route.ts`). Tests hit the Hono route surface (`productsRoute.request(...)`) with `readProduct` and the DB client mocked, covering activation state guards (409), successful multipart activation with background persistence + R2 `put` calls, activation validation failures (missing background / mismatched sizes), deactivate + unlink, direct reactivate, the `missingBackgroundVariantIds` repair contract, repair success with the saved canvas size, and permanent-delete-while-active rejection. Verification: `pnpm --filter backend test` (175 tests / 27 files), `pnpm --filter backend check`, and `pnpm --filter backend build` pass. `feature_list.json` remains pre-existing invalid JSON and was not edited.

- 2026-08-07: Completed the backend Products route composition refactor on main. `products.ts` is now a 34-line Hono facade. `product-query-route.ts` owns list/search and detail reads; `product-command-route.ts` owns create, full-create, core edit, and organization updates; customization and lifecycle remain dedicated capability modules. Public Hono RPC contracts are unchanged. Verification: `./init.sh` passes with 149 backend tests and `git diff --check` passes. Future work, if needed, should split internals of the 646-line command module only around genuine aggregate capabilities, not handler-by-handler wrappers.

- 2026-08-07: Fixed the admin customization Preview render loop. The preview's default `dynamicFonts` value was a new array each render; this retriggered `useBrowserTextMeasure`, whose font-completion state update then rendered again indefinitely. `ProductCustomizationPreview` now has a stable default and the font-measurement effect depends on a content key. Shared typecheck and 13 admin tests pass. Admin build remains blocked by unrelated MISA cleanup errors: `misa-products.tsx` imports removed symbols from `products-client.ts`.

- 2026-08-06: Updated storefront header controls from the supplied TrophySmack cart reference. `Navbar.tsx` renders a compact teal cart control with a white cart icon and restrained orange count badge. The cart `aria-label` is localized through new `navbar_cart` plural strings in `layout.json`. `language-switcher.tsx` now uses a subtle bordered, compact square control with hover and keyboard focus states. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, targeted Prettier check, and `git diff --check` pass. `feature_list.json` remains invalid before this work and was not touched.

- 2026-08-06: Homepage conversion pass is complete. Scope was presentation and localization only: no catalog/backend data was changed, and `QuoteTicker.tsx`, `PartnerLogosSection.tsx`, and `about/product-marquee-section.tsx` were intentionally untouched. Changed home areas are hero, category cards, trust commitments, Vietnamese/English home copy, visible Vietnamese footer legal links, and home carousel accessibility labels. `apps/storefront/app/lib/home-copy.test.ts` protects the concise Vietnamese hero copy and labels. Verification passed: `pnpm --filter router-cf test` (24), `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, targeted Prettier check, and `git diff --check`. Note: after a rebuild, an old local Vite preview process returned 500s for stale assets; do not treat that as a source regression. Start a clean preview only after ensuring no old process is listening on 4173. `feature_list.json` remains invalid before this work and is intentionally untouched.

- 2026-08-06: Storefront PDP regular-media gallery now uses a white stage consistent with the page and customization preview. Multiple gallery images use a compact centered thumbnail rail with a stronger selected state and preserve horizontal scrolling for overflow. Regular media remains borderless, while the customization-only gallery has a subtle inset outline to define its pan/drag canvas without a gray background; individual thumbnail borders remain as the image-selection affordance. Storefront typecheck/build, targeted Prettier check, and `git diff --check` pass. `feature_list.json` remains pre-existing invalid JSON and was not edited.

- 2026-08-06: Storefront PDP customization preview now uses a white viewport that matches the page and gallery background. `ProductCustomizationPreview` takes an optional `viewportClassName`; `ProductDetailLayout` supplies `bg-white bg-none`, intentionally leaving grid defaults unchanged in shared admin/order contexts. Shared check, storefront tests (22), storefront typecheck/build, and `git diff --check` pass. `feature_list.json` remains pre-existing invalid JSON and was not edited.

- 2026-08-06: Refactored `apps/storefront/app/routes/order-lookup.tsx` with a bounded form panel, responsive result surface, loading skeleton, empty state, inline not-found/error alert, accessible labels, `type="tel"` phone input, and shared Button/Input components. Added matching EN/VI lookup strings in `app/locales/*/order-lookup.json`; the Vietnamese product preview action is now `Xem trước`. Lookup API, full-screen customization preview, and order-number URL prefill are unchanged. Verification: 22 storefront tests, typecheck, production build, locale JSON parsing, `git diff --check`, desktop browser review, and a real `Order not found` API error response. `feature_list.json` is pre-existing invalid JSON, so it was not edited.

- 2026-08-06: Storefront typography now uses Inter for UI/body/labels/prices and Barlow Condensed for semantic `font-heading` display surfaces. Changed files: `apps/storefront/app/app.css`, `apps/storefront/app/root.tsx`, `apps/storefront/app/components/home/BestSellersSection.tsx`, `apps/storefront/app/components/product/ProductInfo.tsx`, `apps/storefront/app/components/product/ProductMobileActionBar.tsx`, and `apps/storefront/DESIGN.md`. `QuoteTicker.tsx`, `PartnerLogosSection.tsx`, and `product-marquee-section.tsx` were intentionally untouched. Verification: storefront typecheck/build and `git diff --check` pass; browser inspection confirms the expected computed font families and no horizontal overflow. `feature_list.json` is pre-existing invalid JSON and remains untouched.
- 2026-08-07: Backend Products refactor merged from `refactor/backend-products-route`. The content/options routes, variant batch updates, legacy replacement, individual MISA sync, variant detail/delete/create routes, and variant media routes are composed modules included in the admin Hono RPC build. `product-variant-create-route.ts` owns `POST /:id/variants`; `product-variant-media-route.ts` owns both per-variant media replacement endpoints. `products.ts` is 1,070 lines, down from 3,975. Preserve all endpoint contracts. The next bounded extraction candidates are product customization, lifecycle (publish/archive), and product deletion routes. `./init.sh` passes with 141 backend tests; `git diff --check` passes. `feature_list.json` is invalid JSON from baseline and remains untouched.

- 2026-08-06: Removed `Created` and `Updated` from the Products list sort menu beside Search and deleted the unused `sortBy` state. Verified with `pnpm --filter admin test`, `pnpm --filter admin build`, and `git diff --check`. `feature_list.json` remains pre-existing invalid JSON.

- 2026-08-06: Removed the `Created` and `Updated` filters from `apps/admin/src/pages/products-list.tsx`; the existing sort options were intentionally preserved. Verified with `pnpm --filter admin test`, `pnpm --filter admin build`, and `git diff --check`. `feature_list.json` remains pre-existing invalid JSON.

- 2026-08-06: Admin Products list pagination is now functional in `apps/admin/src/pages/products-list.tsx`. It uses `PAGE_SIZE = 15`, renders `paginatedProducts`, resets on query/filter/sort changes, clamps after result-count changes, and wires Medusa `Table.Pagination` callbacks. Verified with `pnpm --filter admin test`, `pnpm --filter admin build`, and `git diff --check`. `feature_list.json` is pre-existing invalid JSON and remains untouched.

- 2026-07-25: Fixed the home best-sellers image source mismatch. `GET /api/storefront/collections/:handle/products` previously omitted product-level `product_media`, so `home.tsx`'s collection-backed sections could skip the configured product thumbnail and fall back to variant media. `apps/backend/src/routes/storefront/collections.ts` now loads ordered product media and passes it to `buildListingItem`, whose existing priority selects the first product media before variant fallbacks. Added `collections.test.ts` coverage for the product media thumbnail. Verification: backend tests pass (116 tests), backend build passes, storefront build passes, and `git diff --check` passes. Backend check remains blocked only by the existing four missing `id` fields in `apps/backend/src/lib/storefront-product-customization.test.ts`; storefront typecheck reports no TypeScript errors but the RTK wrapper exits with code 1.
- 2026-07-25: Fixed PDP gallery layout shift in `apps/storefront/app/routes/categories.$categoryHandle.products.$productHandle.tsx`. Product images and customization previews now use the same responsive media frame, and the desktop `min-h-[560px]` wrapper was replaced with that shared frame class. Storefront typecheck/build and `git diff --check` pass; Wrangler's debug-log `EPERM` remains sandbox-only.
- 2026-07-25: Category listing refactor completed. The route `apps/storefront/app/routes/categories.$categoryHandle.tsx` now only owns the loader and delegates rendering to `components/categories/CategoryProductsPage.tsx`. `CategoryProductsListing.tsx` owns listing-shell composition, and `useCategoryListingNavigation.ts` owns category/page navigation. Storefront typecheck/build and `git diff --check` pass; the known Wrangler debug-log `EPERM` warning is sandbox-only. Preserve the unrelated existing change in `apps/storefront/app/routes/categories.$categoryHandle.products.$productHandle.tsx`.
- 2026-07-25: Refreshed storefront home best-seller copy: recognition-focused for standard products and personalization-focused for customizable products, with Vietnamese and English content updated in `apps/storefront/app/routes/home.tsx`. `pnpm --filter router-cf build` and `git diff --check` pass; Wrangler emits the known sandbox-only debug-log `EPERM` warning but exits successfully.

- 2026-07-25: Storefront home vertical spacing was tightened in the home section components. `pnpm --filter router-cf build` and `git diff --check` pass; Wrangler emits the known sandbox-only debug-log `EPERM` warning but exits successfully. Preserve the unrelated existing changes in `apps/storefront/app/routes/home.tsx` and `apps/storefront/tsconfig.json`.

- 2026-07-25: Reworked virtual best-sellers ranking. `apps/backend/src/routes/storefront/collections.ts` now treats `best-sellers` as a virtual route: it prioritizes matching products from a `best-sellers` collection when it exists, then ranks remaining published products by total `order_items.quantity` across every created order, then fills with remaining published products. `customizable=all|true|false` applies to all tiers; paging and `total` operate on the combined stream. `collections.test.ts` covers missing collection fallback and source-order preservation. Design doc was committed separately as `9783a4e docs: design best-sellers ranking`. Verification: `pnpm --filter backend test`, `pnpm --filter backend check`, `pnpm --filter backend build`, plus a read-only local D1 ranking query. Backend build prints the known Wrangler EPERM log warning but exits successfully. `./init.sh` reaches backend verification successfully but remains blocked at pre-existing admin Hono RPC and unused-import diagnostics.
- 2026-07-25: Fullscreen read-only customization preview now captures pointer events directly on the visible canvas, so drag-to-pan works even when the pointer starts on the artwork. `pnpm --filter @trophy/customization-react check` passes.
- 2026-07-25: Desktop order customization preview now keeps the artwork and modal header fixed; only the right production sidebar scrolls. The mobile stacked layout keeps whole-modal scrolling for accessibility.
- 2026-07-25: Fixed order-preview font fallback: CSS font requests cannot send the admin app's Bearer token, so the old `/api/admin/brand-assets/fonts/file/:assetId` source was unauthorized. `order-customization-preview-modal.tsx` now uses the existing public storefront font-runtime route. Targeted source assertion and `pnpm --filter backend test -- src/routes/storefront/brand-assets.test.ts` pass (102 tests).
- 2026-07-25: Order customization preview now loads dynamic font families so frozen custom text renders using the correct configured family/variant. It also displays production specifications for every visible text layer (content, font, variant, color hex, Bold/Italic, alignment, type size, path, rotation, placement, and print area). `pnpm --filter admin build` reports no errors from this modal, but remains blocked by existing `auth-client.ts` Hono RPC typing and backend unused-import/type errors.
- 2026-07-25: Refactored `apps/admin/src/pages/order-detail.tsx` into `use-order-detail.ts` (fetch/mutation lifecycle), `order-detail-utils.ts` (pure display and status rules), `order-detail-header.tsx` (header/status/cancel), `order-detail-main-content.tsx` (summary/payment/fulfillment), `order-detail-sidebar.tsx` (customer/production/activity), and `order-customization-preview-modal.tsx` (read-only preview/download workflow). The route container is now 120 lines and owns preview selection only. Prettier and `git diff --check` pass. `pnpm --filter admin build` remains blocked by existing Hono RPC client typing and backend unused-import/type errors; final output has no order-detail diagnostics.
- 2026-07-25: Fixed the recovery endpoint's Better Auth `UNAUTHORIZED`/500 and `FORBIDDEN`/403. Both `apps/backend/src/routes/admin/super-admin.ts` and `accounts.ts` now pass incoming headers to `auth.api.setUserPassword`, and the `super-admin` role now has its required `user:set-password` permission in runtime and CLI auth config. The recovery route has a regression assertion for header forwarding. Rotated the weak local and Worker Better Auth secret to a generated 32-byte value. Restart the backend and sign in again, because existing sessions are invalidated. Backend test/check/build pass (102 tests). Full `./init.sh` is blocked by unrelated admin Hono RPC client type errors and unused imports.
- 2026-07-24: Added explicit, shared customization text-input selection styling in `packages/customization-react/src/index.tsx`: white text on `#288AB6`, with standard and Firefox selectors. This is scoped to the text input and does not depend on either app's Tailwind build. `./init.sh` passes with 98 backend tests.
- 2026-07-24: Fixed shared storefront customization polygon clipping. `packages/customization/src/geometry.ts` converts eligible closed corner-only vectors to literal CSS `polygon(...)`; `packages/customization-react/src/index.tsx` applies it inline as `clipPath` and `WebkitClipPath`. This avoids Tailwind-version differences between admin and storefront. Curved/open vectors still use the existing SVG clip-path fallback. `./init.sh` passes with 98 backend tests.
- 2026-07-24: Fixed shared customization font rendering and controls. Runtime preview font IDs now match injected `@font-face` IDs for both static variants and uploaded dynamic assets. The preview also preloads the regular variant of every configured font family, so each shopper font-choice label renders in its actual family. Shopper forms hide font selection where one usable family remains and show B/I only for uploaded selected-family variants; admin hides and normalizes format policy where no allowed family supports B/I. Storefront and admin font-file endpoints map `:assetId` to `fonts/<assetId>.ttf`; storefront responses are browser-cached with `private, max-age=31536000, immutable`, keyed by immutable upload asset ID. Tests cover shared resolver rules plus storefront/admin font-file contracts; `./init.sh` passes with 98 backend tests.
- 2026-07-18: Fixed Cloudflare same-zone Worker fetch for storefront-to-backend calls. Storefront logs showed backend calls returning `error code: 1042`; Cloudflare documents this as same-zone Worker-to-Worker `fetch` without the required configuration. `apps/storefront/wrangler.jsonc` now includes `global_fetch_strictly_public` next to `nodejs_compat`, allowing the storefront Worker loaders to fetch the backend Worker public URL. Verified with storefront build via `rtk proxy`; storefront typecheck passed when rerun outside the sandbox because sandboxed Wrangler hit `listen EPERM 127.0.0.1`.
- 2026-07-18: Added cross-worker storefront/backend API diagnostics. Storefront `api.non_ok` logs now include response content type and a short response body snippet. Backend `apps/backend/src/app.ts` logs `[backend]` JSON `storefront.request` and `storefront.response` events for every `/api/storefront/*` request before CORS middleware. Deploy both backend and storefront to correlate whether storefront Worker requests actually enter the backend Worker. Verified with storefront typecheck/build and backend check/build via `rtk proxy`; Wrangler still prints the known debug-log EPERM warning but exits successfully.
- 2026-07-18: Added storefront loader/API tracing. `apps/storefront/app/lib/observability.ts` defines structured `[storefront]` JSON logs; `app/lib/api.ts` logs backend requests/responses/errors; layout/home/products/category/collection/product redirect/category PDP loaders log start/success/redirect/error. Use Worker logs and grep for `loader.start`, `category-product-detail`, `fetchStorefrontProduct`, `api.non_ok`, or `api.error` to track PDP backend calls. Verified with storefront typecheck/build via `rtk proxy`; Wrangler still prints the known debug-log EPERM warning but exits successfully.
- 2026-07-18: Standardized storefront backend URL construction. `apps/storefront/app/lib/api.ts` now owns `backendUrl`, asset URL normalization, font URL helpers, and customization image upload. `apps/storefront/app/routes/categories.$categoryHandle.products.$productHandle.tsx` no longer has a local `BACKEND_URL` or hand-built backend upload/font URLs. Verified with storefront typecheck/build via `rtk proxy`; Wrangler still prints the known debug-log EPERM warning but exits successfully.
- Added admin order item production readiness action on 2026-07-18. `Order Item Production Status` in `CONTEXT.md` now includes `not_required`, `pending_review`, and `ready`. Backend route `PATCH /api/admin/orders/:orderNumber/items/:itemId/production` updates a single order item production status and returns refreshed order detail. Admin order detail Production sidebar now shows `Mark ready` for each item still `pending_review`; clicking it marks the item `ready`, refreshes state, and changes the aggregate Production badge to `Ready` once all reviewed items are ready. Verified with backend orders test, backend check/build, and admin build; backend build still prints the known Wrangler EPERM debug-log warning but exits successfully.
- Aligned admin order cancellation payment state on 2026-07-18. `apps/backend/src/routes/admin/orders.ts` and `apps/admin/src/lib/orders-client.ts` now allow `paymentStatus: "cancelled"`. `apps/admin/src/pages/order-detail.tsx` Cancel order sends `paymentStatus: "cancelled"` only when the current payment is still `pending`, making cancelled manual-payment orders stop showing a pending collectible amount. Paid/refunded/failed payment states remain separate for future refund/settlement workflows. Added backend route coverage in `apps/backend/src/routes/admin/orders.test.ts`, updated the schema comment in `apps/backend/src/db/schema.ts`, and removed leftover `Default Sales Channel` copy from order detail. Verified with backend orders test, backend check/build, and admin build; backend build still prints the known Wrangler EPERM debug-log warning but exits successfully.
- Cleaned up admin Orders list domain fields on 2026-07-18. `apps/admin/src/pages/orders-list.tsx` no longer shows fake Region or Sales Channel fields copied from Medusa. `/orders` now lists and exports the real Trophy order fields only: Order, Date, Customer, Status, Payment, Fulfillment, Order Total, and Currency. Add filter now offers Status, Created, Payment, and Fulfillment; Status is backed by `order.status`, while Payment/Fulfillment keep using existing modeled status fields. Verified with `pnpm --filter admin build`.
- Implemented real admin Order detail status actions on 2026-07-18. `apps/backend/src/routes/admin/orders.ts` now exposes `PATCH /api/admin/orders/:orderNumber/status` for existing domain-modeled order status, payment status, and fulfillment status updates, with route coverage in `apps/backend/src/routes/admin/orders.test.ts`. `apps/admin/src/lib/orders-client.ts` adds the status update client, and `apps/admin/src/pages/order-detail.tsx` enables cancel order, mark payment paid/failed/refunded, and fulfillment status actions with refreshed page state and toasts. Customer/address and edit-order mutations remain disabled because Order Address Snapshot and Order Item Snapshot are immutable in `CONTEXT.md`. Verified with focused backend orders tests, backend check/build, admin build, and browser action check marking `ORD-MRHUY1AH-Z5E0` paid via `superadmin/superadmin`; no console errors or warnings. Backend build still prints the known Wrangler EPERM debug-log warning but exits successfully.
- Restored Medusa-style order detail action menus on 2026-07-18. `apps/admin/src/pages/order-detail.tsx` now shows menus for cancel order, edit summary, payment status updates, fulfillment updates, and customer edits, but mutation items remain disabled with a backend-workflow note until real admin order mutation routes exist. Verification: `pnpm --filter admin build` passes.
- Refined admin Order detail against the Trophy order domain model on 2026-07-18. `apps/admin/src/pages/order-detail.tsx` now presents order items as immutable snapshots, fixes duplicate quantity multiplication on line totals, derives paid/outstanding totals from `paymentStatus`, removes fake Cancel/Edit/Fulfill/Capture action menus without backend contracts, uses manual-payment messaging for Manual Payment Orders, labels fulfillment from real `fulfillmentStatus`, adds a Production sidebar summary from item-level `productionStatus`, and adds an accessible `FocusModal.Title` to the customization preview modal. Verified with `pnpm --filter admin build` and browser check at `http://127.0.0.1:5174/orders/ORD-MRHUY1AH-Z5E0` using `superadmin/superadmin`; order detail and preview modal had no console errors or warnings.
- Updated admin Orders list toward Medusa Admin behavior on 2026-07-18. `apps/admin/src/pages/orders-list.tsx` now supports CSV export of filtered rows, Add filter menu/chips for Region, Sales Channel, Created, Payment, and Fulfillment, status/date filtering, Clear all, sorting, a Sales Channel column, linked row cells, and Medusa `Table.Pagination` at 20 rows/page. Verification: `pnpm --filter admin build` passes. Medusa reference list/filter was inspected at `http://localhost:9000/app/orders`; Medusa detail/export routes were unavailable because its local dev server failed dynamic imports. Trophy admin runtime visual check was blocked by unavailable local credentials (`admin@example.com/supersecret` is a Medusa email login, not a valid Trophy username; `admin/supersecret` was rejected).
- Updated storefront cart recently-viewed mobile layout on 2026-07-18. `RecentlyViewedProducts` now uses the shared Carousel pattern with 2 visible items on mobile, 4 visible items on desktop, and left/right actions when scrollable. Verified with storefront typecheck/build via `rtk proxy`.
- Added storefront best-sellers customization filtering on 2026-07-18. Scope: `apps/backend/src/routes/storefront/collections.ts`, `apps/backend/src/routes/storefront/collections.test.ts`, `apps/storefront/app/lib/api.ts`, and `apps/storefront/app/routes/home.tsx`. Collection products API now accepts `customizable=all|true|false`; `best-sellers` page 1 fills missing curated slots with matching published fallback products. Storefront home calls `best-sellers` separately for customizable and regular best-selling sections. Verified with backend test/check/build and storefront typecheck/build via `rtk proxy`; Wrangler still prints the known EPERM debug-log warning but exits successfully.
- Removed uploaded-image rotate actions from `@trophy/customization-react` on 2026-07-18. The selected uploaded-image action dock now keeps zoom, move, and reset controls but no longer shows Rotate left / Rotate right buttons. Verification: direct package TypeScript compile and `pnpm --filter @trophy/customization-react check` pass.
- Fixed desktop fullscreen preview layering in `@trophy/customization-react` on 2026-07-18. `ProductCustomizationPreview` now renders fullscreen mode through `createPortal(..., document.body)` with a top-level overlay z-index, preventing storefront desktop sticky header/PDP layers from sitting above the fullscreen preview. Added package manifest/lock entries for `react-dom` and `@types/react-dom`. Verification: direct package TypeScript compile, `pnpm --filter @trophy/customization-react check` after allowing pnpm network access for its dependency policy check, and `pnpm --filter router-cf build` pass. Wrangler still prints the known EPERM debug-log warning during storefront build but exits successfully.
- Updated storefront home merchandising on 2026-07-18. Scope: `apps/storefront/app/routes/home.tsx`, `apps/storefront/app/components/home/BestSellersSection.tsx`, `apps/storefront/app/components/home/CategoriesSection.tsx`, and `apps/storefront/app/components/shared/ProductCard.tsx`. Home now splits best sellers into customizable and non-customizable sections using the same product-card UI, category section is fixed to the first 4 API categories, category grid has no scroll controls, and mobile remains `grid-cols-2`. Verified with `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` via `rtk proxy`; Wrangler still prints the known EPERM debug-log warning but exits successfully.
- Updated shared customization preview action icons on 2026-07-18. Scope: `packages/customization-react/src/index.tsx`. The preview canvas toolbar now uses `Fullscreen` for the fullscreen action and the distinct `Crosshair` focus icon for fit-to-view, replacing the duplicated `Maximize2` icon while preserving existing behavior and accessibility labels. Verification: `pnpm --filter @trophy/customization-react check` passes.
- Updated storefront checkout mobile summary background on 2026-07-18. Scope: `apps/storefront/app/routes/checkout.tsx`. The mobile order-summary strip now uses the same exact `#F5F5F5` gray as the desktop right-side summary column. Verified with `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` via `rtk proxy`; Wrangler still prints the known EPERM debug-log warning but exits successfully.
- Updated storefront listing hero background on 2026-07-18. Scope: `apps/storefront/app/components/products/ProductListingShell.tsx`. `ListingHero` now uses `/category_bg.jpg` from `apps/storefront/public/category_bg.jpg` as its section background, with dark overlay layers to preserve contrast and the existing featured product image panel unchanged. Verified with `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` via `rtk proxy`; Wrangler still prints the known EPERM debug-log warning but exits successfully.
- Refactored storefront checkout route on 2026-07-18. `apps/storefront/app/routes/checkout.tsx` now keeps behavior ownership in the route but renders through focused internal components: `CheckoutHeader`, `EmptyCheckoutState`, `CheckoutForm`, mobile/desktop order summaries, customer info, payment method, VAT/additional requirements, and submit actions. Mobile and desktop summaries now share summary item/totals/discount components. Verified with `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` via `rtk proxy`; Wrangler still prints the known EPERM debug-log warning but exits successfully.
- Refactored storefront product listing shell on 2026-07-18. `apps/storefront/app/components/products/ProductListingShell.tsx` now has focused internal section components for hero/breadcrumbs, trust badges, filter/result summary, empty state, grid, pagination, and editorial content. The shell keeps the existing route-facing props, composes those sections, and reactivates the existing filter/result summary markup that had been commented out. Verified with `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` via `rtk proxy`; Wrangler still prints the known EPERM debug-log warning but exits successfully.
- Updated storefront checkout layout on 2026-07-18. `apps/storefront/app/routes/checkout.tsx` now uses shared `Container` wrappers for header/empty/error states, while the checkout body uses a Shopify-style 50/50 split. The left form content is constrained and aligned to the center divider; the desktop right order-summary column itself is a full-height `#F5F5F5` region outside any `Container`, with only the sticky summary content constrained inside. Checkout structural lines use `#DEDEDE`; existing shared controls remain in use, and there is no shared `Separator` component in storefront yet. Verified with `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build`; Wrangler still prints the known EPERM debug-log warning but exits successfully.
- Updated storefront cart quantity commit behavior on 2026-07-17. `QuantityInput` now has a `commitOnBlur` mode, and cart uses it so typed values stay local to the field until blur while +/- still update immediately. That prevents temporary large or negative inputs from immediately changing totals or deleting the line item. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Updated storefront listing pagination visibility on 2026-07-17. `apps/storefront/app/components/products/ProductListingShell.tsx` now wraps the shared `Pagination` render in `totalPages > 1`, so `/products` and collection listings no longer show pagination when the API only has one page. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Updated storefront quantity input on 2026-07-17. `apps/storefront/app/components/ui/quantity-input.tsx` now allows the field to be temporarily empty while the shopper edits, keeps raw string input during typing, and only clamps/reset-to-`min` on `blur` when the value is empty or outside `[min, max]`. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Updated the shared customization preview on 2026-07-16. `packages/customization-react/src/index.tsx` now uses a minimal in-canvas overlay model instead of the old top header + fixed action rail: no visible `View/Edit` switch, bottom-right floating `zoom out / zoom in / fit`, bottom-left floating image actions only while an uploaded image is selected, click-empty-canvas to deselect and return to canvas pan, and read-only preview keeps only zoom/pan. Verification: `pnpm --filter @trophy/customization-react check`, `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` all pass.
- Fixed the desktop mega menu grid border gap on 2026-07-16. `apps/storefront/app/components/layout/navbar/mega-menu-grid.tsx` now uses the same reliable grid-line pattern as mobile: wrapper `gap-px bg-border-subtle p-px`, item cells `bg-white`, and no breakpoint-specific `nth-child(...)` border overrides. This avoids responsive missing-border cases when column count changes. Verification: `pnpm --filter router-cf build` passes.
- Implemented storefront recently viewed products in cart on 2026-07-16. New helper `apps/storefront/app/lib/recently-viewed.ts` records PDP visits in `localStorage`, `apps/storefront/app/routes/cart.tsx` now reads/filter-displays up to four recent products through `apps/storefront/app/components/cart/RecentlyViewedProducts.tsx`, and helper tests live in `apps/storefront/app/lib/recently-viewed.test.ts`. Verification passed with `pnpm --filter router-cf test`, `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, `pnpm --filter admin build`, and full `./init.sh`. Small unrelated harness fix: `apps/admin/src/pages/product-detail/product-detail-variants.tsx` now actually renders `variantError`, which removed the TS6133 compile failure blocking admin build.
- Updated storefront product listing UI on 2026-07-16. Scope was `apps/storefront/app/routes/products.tsx`, `apps/storefront/app/routes/collections.$handle.tsx`, `apps/storefront/app/components/products/ProductListingShell.tsx`, `apps/storefront/app/components/products/FilterChips.tsx`, `apps/storefront/app/components/shared/ProductCard.tsx`, and `apps/storefront/app/components/shared/Pagination.tsx`. `/products` now uses the shared TrophySmack-style listing shell; the shell has a dark hero, compact trust row, centered filter strip, 3-column product grid, compact product cards, smaller pagination, and SEO/FAQ content below. Follow-up removed extra inner `px-8` from the filter/category scroll wrappers so Previous/Next buttons align to the existing container instead of shrinking the row twice. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, preview server at `http://localhost:4173/`, and escalated HTTP check for `/products` returning 200 all pass.
- Updated storefront mobile navigation on 2026-07-16. The old `NavbarMobileMenu` fixed overlay was replaced with a drawer component backed by Radix `Dialog` in `apps/storefront/app/components/ui/drawer.tsx` and `apps/storefront/app/components/layout/navbar/mobile-menu.tsx`. The drawer now owns its own `100dvh` scroll container with `overflow-y-auto overscroll-contain`, and the manual `useLockBody` hook was deleted because Radix already handles background scroll lock more safely with the sticky navbar. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Updated storefront category scrolling on 2026-07-16. Scope was `apps/storefront/app/components/home/CategoriesSection.tsx` and the category strip in `apps/storefront/app/components/layout/Navbar.tsx`. Both now use native horizontal scrolling with Previous/Next controls that appear only when the row actually overflows; the navbar strip no longer uses the looping carousel that showed arrows while the visible category row still fit. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass. Wrangler still reports EPERM for debug-log writes under `/Users/dnt/Library/Preferences/.wrangler/logs`, but commands exit successfully.
- Updated storefront home newsletter section on 2026-07-14. Scope was `apps/storefront/app/components/home/NewsletterSection.tsx`, based on the user's pasted TrophySmack newsletter HTML snippet. The component now renders a narrow centered newsletter block with uppercase heading, subheading, 58px email field, and flush right mail-icon submit button using semantic storefront tokens. Verification: `pnpm --filter router-cf typecheck` via `rtk proxy` and `pnpm --filter router-cf build` pass. Wrangler still reports EPERM for debug-log writes under `/Users/dnt/Library/Preferences/.wrangler/logs`, but commands exit successfully.
- Updated storefront home bestseller section on 2026-07-14. Scope was limited to `apps/storefront/app/components/home/BestSellersSection.tsx` and the new `featured` variant in `apps/storefront/app/components/shared/ProductCard.tsx`; the pre-existing dirty `apps/storefront/app/components/home/CategoriesSection.tsx` change was not touched. The section now mirrors the local TrophySmack featured-collection reference with a 1200px frame, desktop `88px` top spacing, centered uppercase heading, 4-up desktop / 2-up mobile carousel, square product image slots, image `sizes` target of `267px` on desktop. Verification: `pnpm --filter router-cf typecheck` via `rtk proxy` and `pnpm --filter router-cf build` pass. Wrangler still reports EPERM for debug-log writes under `/Users/dnt/Library/Preferences/.wrangler/logs`, but commands exit successfully.
- Planned backend absolute asset URL refactor on 2026-07-12. Local issue: `.scratch/issues/backend-absolute-asset-urls-refactor.md`. Next agent should implement from that issue in tiny commits, starting with a backend URL serialization helper that derives the backend origin from the request, then update backend route contract tests before simplifying admin/storefront normalization. `gh` is unavailable, so no GitHub issue was created.
- Added download-all uploaded images in admin order customization preview on 2026-07-12. In the read-only order preview modal, `Download uploads` zips only customer-uploaded image field values from the frozen order snapshot; clipart/preset/background assets are intentionally excluded. The zip is generated client-side with `fflate`, using authenticated admin fetches for backend asset URLs. Verification: admin build, customization-react check, and full `./init.sh` pass.
- Added admin order customization preview on 2026-07-12. Backend `GET /api/admin/orders/:orderNumber` now returns `customization.preview` with the frozen values/template snapshot for customized order items. Admin order detail shows a `Preview` button in the production ticket and opens a read-only FocusModal using `ProductCustomizationPreview readOnly`; only submitted customer values are shown, and no shopper choices can be edited from the order view. `@trophy/customization-react` read-only mode hides Edit/View switching and crop actions while preserving zoom/pan viewing. Verification: focused admin order route test, `pnpm --filter @trophy/customization-react check`, `pnpm --filter admin build`, backend check/test/build, and full `./init.sh` pass.
- Added variant-specific attribute overrides on 2026-07-12. Backend now stores per-variant attributes in `product_variant_attributes` with localized translations under owner type `product_variant_attribute`. Admin product-detail variant modal keeps product attributes as shared defaults and adds a variant override mode that persists localized attribute rows through variant create/edit. Verified with focused backend route tests, backend check/build, admin build, and full `./init.sh`.
- Standardized admin product save-error feedback on 2026-07-12. Create product submit validation/save failures and product detail save/update failures now call `toast.error(...)` with the same message shown inline. Covered overview, organize, attributes, customization, publish/archive, media thumbnail save, variant price/stock/create/edit/delete, and option-save validation. Verification: `pnpm --filter admin build` passes.
- Updated localized product field requirements on 2026-07-12. Backend publish validation now requires product title `vi` only; product title `en`, subtitle, and description are optional and do not block publish by themselves. Docs/spec updated in `CONTEXT.md`, `docs/adr/0006-bilingual-catalog-localization.md`, and `openspec/changes/bilingual-catalog-localization/{design.md,tasks.md,specs/catalog-localization/spec.md}`. Added unit coverage in `apps/backend/src/routes/admin/products.test.ts`. Verification: exact unit file, backend check, and OpenSpec validate pass. Broad `pnpm --filter backend test -- products.test.ts` still hits existing unrelated `products.route.test.ts` query-chain mock failures.
- Fixed admin product-detail Media section on 2026-07-12. `ProductDetailThumbnail` now merges product-level media and unique variant media assets into a product thumbnail/media pool. Product media appears first, variant media remains selectable as thumbnail source media, and separate product-only uploads still work. Multi-file product media upload now works. Verification: `pnpm --filter admin build` passes.
- Updated admin product-detail edit overview on 2026-07-12 to match create-product field requirements: Title requires only Vietnamese, Subtitle and Description are optional localized fields with no missing-locale indicators, and save validates Vietnamese title before update. Verification: `pnpm --filter admin build` passes.
- Updated admin product-detail create/edit variant on 2026-07-12. `apps/admin/src/pages/product-detail/product-detail-variants.tsx` now uses a single-screen `FocusModal` instead of the variant Drawer, with bilingual Title, optional SKU, Options, Price, Media, Inventory quantity, and Allow backorder in one form. Create variants now send bilingual title plus price/inventory/media immediately; editing variants pre-fills those values and persists via the existing operation-specific detail/price/stock/media routes. Backend variant APIs accept legacy string titles or `{ vi, en }` and persist `product_variant` translations. Verification: `pnpm --filter admin build` and `pnpm --filter backend check` pass.
- OpenSpec Goal: `/goal /openspec-apply-change bilingual-catalog-localization`
- Fixed admin product-detail option creation on 2026-07-12. Root cause was a request-body mismatch introduced by the localized option contract: backend `POST /api/admin/products/:id/options` expects `values: [{ value: { vi, en } }]`, while `apps/admin/src/pages/product-detail/product-detail-options.tsx` was still sending `values: [{ vi, en }]`. Updated both the caller and `apps/admin/src/lib/products-client.ts` typing, then added a regression in `apps/admin/src/lib/products-client.test.ts`. Verification: `pnpm --filter admin test -- products-client.test.ts` and `pnpm --filter admin build` pass.
- Replaced the drawer-based multi-option management with a single-option `FocusModal` styled with Medusa UI components on 2026-07-11. Clicking the dropdown menu next to an option triggers the "Edit Option" modal (prefilled with Option title and a tag-input for Variations/Values). Admins can also delete options via the dropdown or add a new option from the top card dropdown. The Variations input allows comma-separated entries, backspace to delete the last badge, and a close button on badges. Verification: `pnpm --filter admin build` and `./init.sh` both pass.
- Admin create-product Organize tab optional controls were updated on 2026-07-11. `apps/admin/src/pages/create-product/create-product-organize.tsx` now marks Collection and Categories optional with Medusa `Label` plus `Text as="span"`, and optional single-select Collection now has a secondary `Clear` button to remove the current selection. Verification: `pnpm --filter admin build` passes.
- Admin create-product required/optional field fix landed on 2026-07-11. Existing docs/specs already say product title is required, subtitle/description/media/attributes are optional, and admin-entered handle is optional with backend generation from title when omitted. Backend schema already matched. Changed `apps/admin/src/components/ui/medusa/localized-field.tsx` to accept `requiredLocales` and wired `apps/admin/src/pages/create-product/create-product-details.tsx` so title marks only Vietnamese required for draft/minimal input while subtitle and description do not show missing-locale indicators. Verification: `pnpm --filter admin build` passes.
- Storefront PDP gallery behavior was corrected on 2026-07-11. `ProductGallery` was already capable of showing thumbnails, but `product.$handle.tsx` incorrectly populated it from `variant.media[0]` across variants, which hid additional images on a single variant. The route now uses the full `selectedVariant.media[]` set as the thumbnail carousel and tracks `selectedMediaId`; the chosen image also drives the customization preview background through `buildProductCustomizationTemplate(...)`. Verification: `pnpm --filter router-cf build` passes.
- Removed the unused legacy storefront file `apps/storefront/app/components/CupCustomizer.tsx` on 2026-07-11. It had no remaining imports/usages, was the last known unreferenced hardcoded-color component under `apps/storefront/app`, and storefront still builds cleanly after deletion. Verification: `rg -n "CupCustomizer" apps/storefront apps/admin packages` returns no matches and `pnpm --filter router-cf build` passes.
- Shared token alignment for `@trophy/customization-react` landed on 2026-07-11. The shared package no longer relies on direct color literals; it now consumes generic semantic roles (`accent`, `accent-foreground`, `surface-container-low`, `surface-container`, `surface-container-lowest`, `outline`, `outline-variant`, `on-surface`, `on-surface-variant`, `destructive`). `apps/admin/tailwind.config.ts` was updated to expose the same vocabulary the storefront already provides, so the shared package renders consistently in both consumers. Verification: `pnpm --filter @trophy/customization-react check`, `pnpm --filter admin build`, and `pnpm --filter router-cf build` pass.
- Latest storefront localization follow-up landed on 2026-07-11. The storefront no longer renders bilingual catalog objects directly: API types accept localized text values, home/category cards, navbar/mega/mobile menus, listing cards, collection titles/descriptions, PDP option/spec text, cart, and checkout normalize through `getLocalized(...)`, and relevant loaders pass the active locale to catalog API calls. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Latest storefront listing UI follow-up landed on 2026-07-11. `/products` and `/collections/:handle` now share `apps/storefront/app/components/products/ProductListingShell.tsx`, which implements the screenshot-driven collection/listing treatment: dark hero, trust badges, structured filter bar, denser 2/3/4-column product grid, and numbered pagination. `products.tsx` also switched `?category=` to use category handles directly instead of category names. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and preview at `http://localhost:4173/`.
- `pnpm --filter router-cf dev` currently fails in this environment with `React Router Vite plugin not found in Vite config`; use `pnpm --filter router-cf preview -- --port 4173` for local UI review unless the Vite dev config is fixed.
- Storefront semantic-token cleanup landed on 2026-07-11. `apps/storefront/app/app.css` now defines semantic Tailwind color tokens for brand/text/surface/border/action roles, `tailwind.config.js` mirrors them, and the current storefront shell/home/shared/product/cart/checkout surfaces were refactored off direct hex classes. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and targeted `rg` checks show no old TrophySmack hex literals in the touched surface set.
- Final screenshot-driven TrophySmack color-role pass landed on 2026-07-11. Supportive CTAs now use blue (`#288AB6`), payment/success states use teal (`#04A387`), and high-intent purchase/listing-price accents use orange (`#EA4222`). This was applied to `HeroSection`, `NewsletterSection`, `ProductCard`, `cart.tsx`, `checkout.tsx`, and documented in `apps/storefront/DESIGN.md`. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- TrophySmack storefront redesign was continued on 2026-07-11 across the shopper shell and home/shared UI. Navbar, footer, hero, trust bar, home merchandising sections, shared product cards, pagination, and filter chips now follow the same TrophySmack palette and display/body font system as product detail, cart, and checkout. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- TrophySmack storefront alignment was extended on 2026-07-11 into cart and checkout. `apps/storefront/app/routes/cart.tsx` and `apps/storefront/app/routes/checkout.tsx` now use the same TrophySmack display/body fonts and the orange/teal/dark-blue hierarchy already introduced at the token level and product detail. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Storefront design system was aligned on 2026-07-11 to the local TrophySmack reference at `/Users/dnt/Desktop/www.trophysmack.com/www.trophysmack.com/index.html`. `apps/storefront/app/root.tsx` now loads `Koulen` + `Open Sans`; `apps/storefront/app/app.css`, `apps/storefront/tailwind.config.js`, and `apps/storefront/DESIGN.md` now reflect the TrophySmack palette and typography system; product detail CTA/price/option typography was tuned to match. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Storefront palette was simplified again on 2026-07-11. Removed the champagne-metal accent from the canonical design system and remapped storefront tokens to a stricter white/ink/graphite/mineral set in `apps/storefront/DESIGN.md`, `apps/storefront/app/app.css`, and `apps/storefront/tailwind.config.js`. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Storefront white-luxury design direction was tightened on 2026-07-11. `apps/storefront/DESIGN_2.md` now differs materially from `DESIGN.md`: true white background, ink-first CTA strategy, champagne-metal accent used sparingly, cooler support text, mineral borders, lower shadow reliance, and 1680px workbench product-detail layout. Implementation was synced in `apps/storefront/app/app.css`, `tailwind.config.js`, `app/components/product/ProductBreadcrumbs.tsx`, `ProductGallery.tsx`, `ProductInfo.tsx`, `ProductMobileActionBar.tsx`, and `app/routes/product.$handle.tsx`. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Design docs were consolidated on 2026-07-11: `apps/storefront/DESIGN_2.md` was merged into `apps/storefront/DESIGN.md` and then removed. `DESIGN.md` is now the single canonical storefront design spec.
- Storefront product detail visual design was unified on 2026-07-11. Breadcrumbs, gallery shell, customization purchase panel, option/quantity controls, mobile action bar, and lower detail/trust sections now share a white workbench background, max-width 1680px content frame, 14px shell radius, warm tan borders (`#d8c1ad`), deep ink CTA (`#110023`), blue-gray labels (`#2d4056`), and muted copy (`#7b6b5f`). Product data flow, customization runtime, upload handling, validation, and cart behavior were unchanged. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass. Storefront dev server restarted at `http://localhost:5175/`.
- Shared customization preview toolbar layout was updated on 2026-07-11 to avoid layout shift when selecting or deselecting uploaded images. In `@trophy/customization-react`, the header now stays stable with mode and viewport zoom controls, and a fixed 76px sticky action rail directly below it shows either guidance or the selected image crop/rotate/nudge/reset actions. Verification: `pnpm --filter @trophy/customization-react check`, `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass. Storefront dev server restarted at `http://localhost:5175/`.
- Storefront Tailwind generation for `@trophy/customization-react` was fixed on 2026-07-11. Admin uses Tailwind v3 `content` and already included the shared package; storefront uses Tailwind v4 in `apps/storefront/app/app.css`, so it now declares `@source "../../../packages/customization-react/src";`. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass. Dev server restarted at `http://localhost:5175/`.
- Shared customization form UI was restyled on 2026-07-11 to follow the provided mug customization reference screenshots, while the preview canvas/editor was intentionally left unchanged. `@trophy/customization-react` is the shared package for the form used by storefront product detail and admin PreviewDialog. It now provides large plain labels, responsive 56px-style inputs, big color/font swatches, and upload thumbnail + Replace/remove controls with admin-safe responsive sizing. Existing data contracts, upload endpoint, crop/edit behavior, validation, and cart snapshot logic were preserved. Verification: `pnpm --filter @trophy/customization-react check`, `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter admin build` pass.
- Customization React UI was extracted to `@trophy/customization-react` on 2026-07-10. Storefront product detail imports the shared package preview/form and supplies upload/font callbacks; admin PreviewDialog also uses the shared package canvas/form while keeping admin-only reset/export/close shell controls. The old storefront-local customization component was deleted and the old admin-local preview/form implementation was removed. Verification: `pnpm --filter @trophy/customization-react check`, `pnpm --filter @trophy/admin build`, `pnpm --filter router-cf typecheck`, `pnpm --filter router-cf build`, and `pnpm --filter customization check` pass.
- Added storefront GitHub CI/CD on 2026-07-10 in `.github/workflows/storefront-ci-cd.yml`. PRs touching storefront/customization now run checks and upload a Workers preview with alias `pr-<number>`, then post/update a PR comment with the preview URL. Pushes to `main` rerun checks and deploy production with Wrangler. `apps/storefront/wrangler.jsonc` now sets `preview_urls: true`. Before relying on the workflow in GitHub, add repo secrets `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, and `STOREFRONT_BACKEND_URL`.
- Storefront preview image actions were moved to the preview top toolbar on 2026-07-10. They only render in Edit mode after selecting an uploaded image; the canvas keeps the selected-image outline and four scale handles without the previous floating action panel. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Latest storefront product detail follow-up on 2026-07-10 rebuilt the preview/system design into a personalization studio. Preview now supports Edit/View mode switching, fit/zoom controls, View-mode pan/pinch zoom, and admin-like image editing in Edit mode: click uploaded image to select, drag crop, wheel zoom, use floating zoom/rotate/nudge/reset controls, and drag four corner handles to scale. Gallery/setup/customization surfaces were restyled into a unified workflow. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter customization test`, `pnpm --filter customization check`, and `pnpm --filter router-cf build` pass.
- Latest storefront customization image-editing follow-up on 2026-07-10 moved image adjustment actions into the preview canvas. The form now only uploads/replaces/removes image values; the selected uploaded image in preview shows a floating toolbar for zoom, rotate, nudges, and reset, plus direct drag-to-pan. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter customization test`, `pnpm --filter customization check`, and `pnpm --filter router-cf build` pass.
- Storefront product detail received a follow-up refinement on 2026-07-10: variant selection is grouped by product option and hides single-value options; customization form area no longer contains variant selection; uploaded image customization supports direct preview drag plus zoom/rotate/move/reset controls. Shared customization values now include optional `cropRotationDeg`. Verification: `pnpm --filter router-cf typecheck`, `pnpm --filter customization test`, `pnpm --filter customization check`, and `pnpm --filter router-cf build` pass.
- Storefront product detail was redesigned on 2026-07-10 with a personalization-first layout in `apps/storefront/app/routes/product.$handle.tsx`, `ProductGallery`, `ProductInfo`, and `ProductCustomization`. Data flow and backend contracts are unchanged. Verification: `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build` pass.
- Admin customization Preview field layout was updated on 2026-07-10 in `apps/admin/src/components/customization/customization-template-preview.tsx`. Scope was intentionally limited to `PreviewField` presentation: field cards, styled upload picker, compact crop buttons, larger clipart grid, and grouped text style controls. `pnpm --filter admin build` passes.
- `catalog_translations` table schema was added.
- `upsertTranslations` and `hydrateTranslations` helper functions implemented in `apps/backend/src/lib/catalog-translation.ts`.
- `admin/products.ts` and `admin/product-metadata.ts` were updated to read and write translations.
- Replaced `string` definitions with `localizedString` schema across `optionCreateSchema`, `fullCreateProductSchema`, etc.
- The `backend` code successfully passes `pnpm --filter backend check`.
- `vitest` unit/integration tests for `orders.test.ts` and `products.route.test.ts` are failing. This is because these integration tests use a custom mock (`createQueryChain`) that hardcodes the expected queries for `.push()` mutations and expects the exact sequence of `db.insert(...)` and `db.update(...)` queries. The `catalog_translations` queries and the updated BetterAuth query flow in `orders.test.ts` break these rigid expectations.
- 2026-07-17: Storefront PDP breadcrumb now lives in its own full-width centered bar below the navbar. Files touched: `apps/storefront/app/components/product/ProductBreadcrumbs.tsx` and `apps/storefront/app/routes/product.$handle.tsx`. Verified with `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build`; Wrangler still prints the known EPERM log-write warning but exits successfully.
- 2026-07-17: Storefront category navigation now uses path-based URLs. Category listing route: `/categories/:categoryHandle`; PDP route inside category: `/categories/:categoryHandle/products/:productHandle`; old `/product/:handle` redirects to the product's primary category path. Navbar desktop strip and mobile menu underline the active category from the current pathname. Verified with `pnpm --filter router-cf typecheck` and `pnpm --filter router-cf build`.
- 2026-07-25: Super-admin recovery is implemented but unverified. Restore dependencies with `pnpm install --no-frozen-lockfile` when network is available, then run `pnpm --filter backend test`, `pnpm --filter backend check`, `pnpm --filter backend build`, `pnpm --filter admin build`, and `./init.sh`. New route surface: public `GET /api/admin/onboarding/status`; authenticated `POST /api/admin/super-admin/recovery`; Team consumes guarded `/api/admin/accounts` routes through Hono RPC.
- 2026-07-25: Storefront typography is now configured around an IBM-inspired universal scale directly in the Tailwind CSS v4 `@theme` block in `apps/storefront/app/app.css`. Existing `text-*` typography classes receive the approved values without component changes. `apps/storefront/DESIGN.md` documents the role table and IBM source. The scale is display 76/86, 68/78, 60/70, 54/64; headline 42/50, 32/40, 28/36; title 24/28, 20/26, 18/24; body 18/24, 16/22, 14/20; label 14/20 and 12/16. Build and generated CSS inspection pass. Follow-up work, if desired, is migrating remaining ad-hoc `text-[...]` and `leading-*` classes to these role tokens. Typecheck is blocked by existing TS5102 `baseUrl` errors in `tsconfig.node.json` and `tsconfig.cloudflare.json`.
- 2026-07-25: `BestSellersSection.tsx` now uses `text-headline-md md:text-headline-lg` for the section heading and `font-body-md text-body-md` for the subtitle. The prior arbitrary `text-[30px]`, `text-[40px]`, `text-[15px]`, `leading-none`, and `leading-7` utilities were removed; layout and carousel behavior are unchanged. Storefront build passes.

# Current Session

- 2026-07-26: The five oversized React entry surfaces retain stable public paths and delegate feature-local implementation. This is now a real section-level refactor for Inspector (canvas/text/image sections), Panels (blocks/layers/form/background sections), Variants (shared price/stock batch drawer), and Preview (props-only toolbar); Category Detail still needs its data hook plus create/detail view split. No behavior or contract changed. Admin build, shared check, storefront build, and diff check pass after the section refactors.

- 2026-07-26: Product Detail Variants now has a real hook boundary: `use-product-detail-variants.ts` owns local UI state plus product-client save/upload/delete flows, while `product-detail-variants-feature.tsx` only composes the view. The component is 565 lines (from 936); the hook is 399 lines, and the shared batch drawer is 76 lines. Admin tests (12) and full `./init.sh` pass.

- 2026-07-26: Added the shared storefront `Container` component to `apps/storefront/app/routes/order-confirmation.tsx`, preserving the existing confirmation page behavior. `pnpm --filter router-cf build` and `git diff --check` pass.

- 2026-07-26: `./init.sh` passes end to end after clearing the admin build diagnostics. The fixes remove unused symbols in backend/admin code and use a browser-compilation-safe broad binding surface in `apps/backend/src/lib/env.ts` for Hono RPC types; Wrangler remains the deployment contract for concrete Cloudflare bindings.

- 2026-07-26: Fixed early text trimming in `packages/customization-react/src/index.tsx` and `apps/admin/src/components/customization/customization-template-editor.tsx`. The preview and editor now use Canvas `measureText` for the resolved font instead of a fixed character-width estimate and rebuild after font loading. The reported fixed-size `Imperial_Script` layer no longer trims the final `4` when its actual glyphs fit the 333px boundary. Added a core regression test. Shared check, customization test (35 tests), storefront build, and diff check pass. Admin build is blocked only by existing unrelated auth-client/backend/shared-package diagnostics.

- 2026-07-25: Backend product search now normalizes Vietnamese text (`Áo`/`áo`/`AO` match), searches canonical and translated title/subtitle plus handle/category, and treats the query as required tokens rather than one contiguous phrase (`áo đông` matches `Áo khoác mùa đông`). Added coverage in `apps/backend/src/routes/storefront/products.test.ts`. Targeted backend tests (115) and backend build pass; backend check is blocked only by pre-existing missing `id` fields in `src/lib/storefront-product-customization.test.ts` fixtures.

- 2026-07-25: Storefront navbar search is now backend-backed. `apps/storefront/app/hooks/useSearch.ts` calls the existing `/api/storefront/products?q` and `/api/storefront/categories` APIs after the existing 400ms debounce, maps localized names and real product media/prices, caps each suggestion group at eight, and cancels stale updates. `SearchResults.tsx` uses `SearchProduct`/`SearchCategory` types instead of mock types. Verification: storefront typecheck, build, and `git diff --check` pass; Wrangler emits the known sandbox-only log-write `EPERM` warning but exits successfully. No backend contract change was made.

- 2026-07-25: Implemented the unified admin customization canvas interaction in `apps/admin/src/components/customization/customization-template-editor.tsx`. Product Detail and Create Product both use this shared canvas: there is no Edit/View toggle; layer pointer events select and own layer drag/edit interactions, while the canvas background clears layer/path/point selection and starts viewport pan. Locked layers remain selectable but immovable. Polygon drawing and the separate shopper `PreviewDialog` are unchanged. Verification: `git diff --check` passes. `pnpm --filter admin check` is unavailable because the admin package has no `check` script; `pnpm --filter admin build` is blocked by pre-existing auth-client/backend/shared-package diagnostics and reports no new error in the changed canvas file. Design doc: `docs/plans/2026-07-25-admin-customization-canvas-interaction-design.md`. Git commit was attempted but blocked by permission denied creating `.git/index.lock`.
- 2026-07-25: Admin order detail production actions were moved beside each customization `Preview` button and added to the preview modal. The sidebar duplicate was removed. `OrderDetailPage` derives the active modal item from the latest order snapshot so marking ready updates the modal immediately. Changed files: `apps/admin/src/pages/order-detail.tsx`, `apps/admin/src/pages/order-detail/order-detail-main-content.tsx`, `apps/admin/src/pages/order-detail/order-detail-sidebar.tsx`, and `apps/admin/src/pages/order-detail/order-customization-preview-modal.tsx`.
- 2026-07-25: Added `Mark pending` for ready items beside `Preview` and in the preview modal, using the existing production update API to restore `pending_review` after an accidental ready action.
- Verification: `git diff --check` passes. `pnpm --filter admin build` is blocked by existing `src/lib/auth-client.ts` Hono RPC typing errors and backend unused-import/type diagnostics; it reported no remaining diagnostics in the changed order-detail files after removing the unused sidebar prop.
- 2026-07-25: Admin customization Inspector Polygon Vector Points no longer has a nested scroll container; removed `max-h-48 overflow-y-auto` from `apps/admin/src/components/customization/customization-template-inspector.tsx` so the Inspector tab handles scrolling. `git diff --check` passes. Admin build remains blocked by pre-existing diagnostics.
- 2026-07-25: Polygon vector point selection is now shared between canvas and Inspector. Clicking a point highlights and scrolls its Inspector card into view across template, product, and embedded create-product editors. `git diff --check` passes; admin build remains blocked by pre-existing diagnostics.
- 2026-07-25: Storefront polygon parity fix: `packages/customization/src/geometry.ts` no longer converts vectors with `cornerRadius` into lossy CSS `polygon(...)`; it uses the existing SVG clip-path fallback, matching admin. Regression test passes along with customization check and storefront build.
- 2026-07-25: Verified persistence and stale-data hypotheses. Admin product customization saves polygon geometry unchanged to `layersJson`; storefront sanitizer preserves it. Added no-store cache controls to storefront product detail fetch/response. Targeted backend tests pass (113); backend check is blocked only by four pre-existing missing-media-id fixture errors.
- 2026-07-25: Storefront vector uploads now render inside an inline SVG clip context. `PreviewImageShape` uses a same-component SVG `<image>` plus `clipPathUnits="userSpaceOnUse"`, so overflow/crop transforms cannot break polygon clipping. Shared React check and storefront build pass.
- 2026-07-25: Admin Create Product now uses Medusa `toast.error` for every error path. Inline errors were removed from `create-product.tsx`, Details, Organize, and Variants. Validation messages list all applicable problems by section and tell the admin exactly what to edit to save as draft or publish. Upload, metadata, customization gating, and save errors include specific recovery instructions while retaining form state.
- Verification: `pnpm --filter admin test -- --runInBand` passes with 4 files and 12 tests; Prettier and `git diff --check` pass. `pnpm --filter admin build` is still blocked by pre-existing `src/lib/auth-client.ts` Hono RPC errors plus backend binding/unused-import and shared-package diagnostics.
- Relevant files: `apps/admin/src/pages/create-product/use-create-product.ts`, `apps/admin/src/pages/create-product.tsx`, `apps/admin/src/pages/create-product/create-product-details.tsx`, `apps/admin/src/pages/create-product/create-product-organize.tsx`, and `apps/admin/src/pages/create-product/create-product-variants.tsx`.
- 2026-07-25: Product Detail now uses Medusa toast errors throughout. Removed inline errors and backing error state from overview, organize, attributes, options, variants, media, customization, publish/archive, and product loading. Toast descriptions include the failed operation plus the concrete correction/retry action; no `InlineError` references remain under Product Detail.
- Verification: `pnpm --filter admin test -- --runInBand` passes with 4 files and 12 tests; `git diff --check` passes. `pnpm --filter admin build` remains blocked by pre-existing `auth-client` Hono RPC, backend binding/unused-import, and shared-package diagnostics outside Product Detail.
- 2026-07-26: Storefront customization watermark is implemented in `packages/customization-react/src/index.tsx`. It defaults off in the shared `ProductCustomizationPreview`; storefront product detail and order lookup pass `watermark`, while admin call sites omit it. Admin and customization/order data are unchanged. Shared React check, storefront typecheck/build, and `git diff --check` pass. Existing unrelated refactor changes remain in the working tree, including admin order-detail extraction and shared customization files.
- 2026-07-26: React component architecture refactor is partial. Completed focused splits for admin brand asset modals and order customization preview helpers. Continue with the remaining >550-line files listed in `progress.md`; preserve unrelated storefront and shared customization changes already present in the working tree.
- 2026-07-26: Refactor continued with `order-detail-sections.tsx`, `customization-react/preview-layers.tsx`, and `customization-react/customization-forms.tsx`. These preserve existing props/state ownership. Verification passed for `pnpm --filter @trophy/customization-react check` and `pnpm --filter customization test` (35 tests). Remaining large files are unchanged and listed in `progress.md`.
- 2026-07-26: Runtime export bug fixed: `ProductCustomizationForm` and `CustomizationStudio` are explicitly re-exported from the shared package entrypoint after form extraction. `pnpm --filter @trophy/customization-react check` and `pnpm --filter router-cf build` pass.
- 2026-07-26: Continued the oversized-component refactor by extracting `FontLoader`, `CustomizationWatermark`, and `CanvasAction` to `packages/customization-react/src/preview-support.tsx`. The shared package check and customization tests pass. Remaining >550-line files are recorded in `progress.md`; `index.tsx` remains above the threshold because the form-field bodies and preview component still need a focused extraction.
- 2026-07-26: Completed the shared customization entrypoint extraction. `TextField`, `ImageField`, and `ProductCustomizationPreview` now live in `text-field.tsx`, `image-field.tsx`, and `product-customization-preview.tsx`; `index.tsx` is 311 lines. Public aliases and the `ProductCustomizationForm` export remain intact. Shared check, 35 customization tests, storefront build, and diff check pass. Remaining >550 files are listed in `progress.md`.
- 2026-07-26: Completed the `customization-template-editor.tsx` refactor. The coordinator is 549 lines; vector point/handle behavior is in `customization-template-editor-vector.tsx` and text/path/resize overlays are in `customization-template-editor-text.tsx`. Callback contracts and canvas interactions are unchanged. Shared check and customization tests pass. Admin build remains blocked by pre-existing auth-client/backend diagnostics, with no error reported from the changed editor modules.
- 2026-07-26: Fixed the Hono RPC auth contract. `adminBootstrapRoute` is chained for route inference, and JSON validators now expose typed bodies for bootstrap, account creation, password reset, and super-admin recovery. Backend check/build pass and all 116 backend tests pass. The previous `auth-client.ts` bootstrap/json errors are gone from admin build; remaining build diagnostics are unrelated existing backend bindings/imports plus an unused `ArrowLeft` import.
- 2026-07-26: Fixed the order customization preview media mapping. New order snapshots now prefer the selected variant's customization media asset for `backgroundSnapshot`, falling back to the first regular variant media asset. Added regression coverage in `storefront/orders.test.ts`; backend check/build and 116 tests pass.
- 2026-07-26: Added a historical-order fallback in `apps/backend/src/routes/admin/orders.ts`: customized order items now resolve the variant's customization media when the stored snapshot was created with regular gallery media. This covers both new and previously created orders. Admin/storefront order route tests pass.
- 2026-07-26: Cart and checkout thumbnail fix completed. Backend resolve prefers ordered product-level media and falls back to variant media, so both `apps/storefront/app/routes/cart.tsx` and checkout `OrderSummary` receive a representative image even for products without variant gallery media. The storefront product-detail response now includes product media, and `use-product-detail-state.ts` stores that fallback for new cart lines. Regression test is in `apps/backend/src/routes/storefront/orders.test.ts`. `./init.sh` passed end to end with 117 backend tests, backend check/build, admin build, storefront typecheck/build, and diff check.
  2026-07-26: Rebuilt MISA integration under OpenSpec change `misa-product-order-sync`. Current code includes MISA client/proxy, publish product sync with stored IDs, guarded delete, standalone `/misa-products`, and post-checkout contact/sale-order sync. Schema fields are present but migration is intentionally not generated; operator must run `pnpm --filter backend db:generate` and apply it. `./init.sh` passes with 127 backend tests. Archive remains out of scope.

2026-08-03: Added `other-products` as a system fallback category. `POST /full-create` and `PATCH /:id/organize` create or reuse it when category input is empty, product metadata exposes it as `isSystem`, and the category API blocks delete/name/handle/description changes for it just like `customization`. Admin category detail therefore hides the Delete action through its existing `isSystem` conditional. Verification passed with `./init.sh` (137 backend tests, all app checks/builds) and `git diff --check`. `feature_list.json` is invalid JSON before this work, so it was not updated.
- 2026-08-07: Admin navigation cleanup is complete. `apps/admin/src/App.tsx` no longer registers `inventory`, `customization/templates/*`, or `misa-products`; `apps/admin/src/lib/sidebar-config.ts` no longer exposes those links and Customization defaults to `/customization/clipart`. Removed the unused standalone page files and MISA fetch helper. Verification passed: `pnpm --filter admin test` (13), `pnpm --filter admin build`, full `./init.sh` (148 backend tests plus all checks/builds), `git diff --check`, and source reference scan. `feature_list.json` is pre-existing invalid JSON and remains untouched.
## 2026-08-07 — Storefront not-found page

- Implemented the storefront 404 experience in `apps/storefront/app/routes/catchall.tsx`.
- Nested the catch-all route inside `storefront-layout` in `apps/storefront/app/routes.ts`, preserving the loader's HTTP 404 status while adding the shared shell.
- Added localized `notFound` strings to `apps/storefront/app/locales/en/common.json` and `apps/storefront/app/locales/vi/common.json`.
- Verified with storefront tests (26), typecheck, build, `git diff --check`, and a live unknown-URL request returning HTTP 404.
- Design and implementation plan: `docs/plans/2026-08-07-storefront-not-found-design.md` and `docs/superpowers/plans/2026-08-07-storefront-not-found.md`.
- `feature_list.json` is pre-existing invalid JSON and remains untouched.
# R2 Media Ownership Design — 2026-08-07

The R2 design review is documented in `docs/adr/0009-shopper-draft-asset-retention.md` and terminology is in `CONTEXT.md`. It is a design-only change; no production code was edited or verified. Implement future work from the ADR: semantic new-object keys only (no migration), order-owned copies only for customized item background/uploads/clipart, seven-day shopper-draft lifecycle with failed-transfer protection, and retryable order-media transfer failures. Current backend has no scheduled Worker handler or cron configuration. Current permanent product deletion does not remove `product_assets` or R2 objects. `feature_list.json` is invalid baseline JSON; leave it untouched unless repaired separately.
