# MISA variant-ID product codes

MISA products are identified by the stable Trophy variant ID rather than SKU. This keeps the external mapping stable when operators add or edit SKUs, allows SKU-less variants to synchronize, and makes per-variant MISA status and deletion unambiguous.
